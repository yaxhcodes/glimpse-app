import 'package:flutter/material.dart';
import '../glimpses/glimpses_screen.dart';

/// Retains the established Rediscover route and launcher entry point.
class RediscoverScreen extends StatelessWidget {
  const RediscoverScreen({super.key});

  @override
  Widget build(BuildContext context) => const GlimpsesScreen();
}
